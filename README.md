This project contains automated tests for the TransLink website using Selenium WebDriver and TestNG.

Prerequisites

Java JDK 8 or Later
Maven(build management, required dependencies stored in POM.xml can be cloned with project)
Chrome

Git clone 
